<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nadee.navigation_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutUs"
            android:label="@string/title_activity_about_us"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MorningPulse"
            android:label="@string/title_activity_morning_pulse"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Reports"
            android:label="@string/title_activity_reports"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SessionChartsActivity" />
        <activity android:name=".HealthImpTechActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".LogIn" />
        <activity android:name=".Register" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".SessionCalender" />
        <activity
            android:name=".LogOut"
            android:label="@string/title_activity_log_out"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddMealActivity"></activity>
    </application>

</manifest>